kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: project-catalogue-cluster
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    extraPortMappings:
      - containerPort: 80
        hostPort: 19080
        protocol: TCP
      - containerPort: 443
        hostPort: 19443
        protocol: TCP

